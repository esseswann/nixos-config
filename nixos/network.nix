{
  networking = {
    hostName = "blackbox";
    networkmanager.enable = true;
    # wireless.networks = {
    #   enable = true;
    #   ziferblat = { psk = "allyouneedislove"; };
    #   tool = { psk = "lateralus"; };
    # };
  };
}
